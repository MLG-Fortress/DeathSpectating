name: CI/CD

on:
  push:
    branches: ['**']
  pull_request:
  release:
    types: [published]

jobs:
  ci:
    permissions:
      contents: write
    uses: MLG-Fortress/.github/.github/workflows/maven-ci.yml@master
    with:
      java-version: '25'
